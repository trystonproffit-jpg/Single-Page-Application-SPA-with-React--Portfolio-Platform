function ProjectList({ projects }) {
    return (
        <>
            {projects.map(project => (
                <div key={project.id}>
                    <h3>{project.title}</h3>
                    <p>{project.description}</p>
                </div> 
            ))}
        </>
    );
}

export default ProjectList;